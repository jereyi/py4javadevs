my_set.remove(2) # Removes 2 from set, throws error if not present
my_set.discard(3) # Removes 3 from set, does not throw error if not present
popped_element = my_set.pop() # Removes a random element from set 
